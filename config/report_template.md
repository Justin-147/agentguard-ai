# AgentGuard AI Assessment | {{ workflow.name }}

## Executive Summary
- Overall risk level: **{{ result.overall_risk_level }}**
- Overall score: **{{ "%.2f"|format(result.overall_score) }}**
- Top risk drivers: {{ top_risk_drivers | join(", ") if top_risk_drivers else "No material findings" }}
- Most urgent control gaps: {{ urgent_control_gaps | join(", ") if urgent_control_gaps else "No urgent gaps identified" }}

## Workflow Overview
| Field | Value |
|---|---|
| Workflow ID | {{ workflow.id }} |
| Business domain | {{ workflow.business_domain }} |
| Agent type | {{ workflow.agent_type }} |
| Description | {{ workflow.description }} |
| Tools | {{ workflow.tools | length }} |
| Data access categories | {{ data_categories | join(", ") if data_categories else "None" }} |
| External actions | {{ workflow.external_actions | length }} |
| Human oversight | {{ "Enabled" if workflow.human_oversight.human_in_the_loop else "Not enabled" }} |
| Generated at | {{ generated_at }} |

## Risk Findings
| Risk | Severity | Likelihood | Score | Affected Component | Recommended Controls |
|---|---|---|---|---|---|
{% for finding in result.findings -%}
| {{ finding.risk_tag }} | {{ finding.severity }} | {{ finding.likelihood }} | {{ "%.2f"|format(finding.risk_score) }} | {{ finding.affected_component }} | {{ finding.recommended_controls | join(", ") }} |
{% else -%}
| None | low | low | 0.00 | N/A | N/A |
{% endfor %}

## Detailed Findings
{% for finding in result.findings %}
### {{ finding.title }}
- Risk tag: `{{ finding.risk_tag }}`
- Severity / likelihood / score: {{ finding.severity }} / {{ finding.likelihood }} / {{ "%.2f"|format(finding.risk_score) }}
- Affected component: {{ finding.affected_component }}
- Evidence:
{% for item in finding.evidence %}
  - {{ item }}
{% endfor %}
- Why it matters: {{ finding.description }}
- Recommended controls: {{ finding.recommended_controls | join(", ") if finding.recommended_controls else "None mapped" }}
{% else %}
No material findings were generated by the deterministic rules.
{% endfor %}

## Control Gap Summary
### Missing Controls
{% for gap in result.control_gaps %}
- {{ gap }}
{% else %}
- No missing controls were identified.
{% endfor %}

### Existing Strengths
{% for strength in result.strengths %}
- {{ strength }}
{% else %}
- No explicit strengths were identified.
{% endfor %}

## Recommended Next Steps
{% for recommendation in result.recommendations %}
- {{ recommendation }}
{% else %}
- Continue periodic review as the workflow changes.
{% endfor %}

## Disclaimer
This assessment is a technical prototype for AI agent governance review. It is not legal, compliance, financial, or security certification advice.
